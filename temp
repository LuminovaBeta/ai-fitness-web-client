基于我们前面进行的一系列深度重构和业务完善，你的后端系统已经具备了极高的工程成熟度（涵盖了异步处理、轮询机制、动静数据分离等）。

我为你重新编写了 `README.md` 中 **“贰、 API 接口列表设计”** 以及 **“伍、 后端业务框架”** 的部分。你可以直接将以下内容替换到你的 `README.md` 文件中对应的章节。

---

## 贰、 API 接口列表设计 (RESTful)

基于 Django REST Framework + Simple JWT 实现，业务接口高度解耦。针对边缘硬件的高低频数据特性，采用了**异步处理与长轮询结合**的架构。

### 模块 1：设备驻留与视觉认证 (Auth & Face CV)

* `POST /api/auth/register/`：**基础账密注册**。仅负责创建 User 和 UserProfile，并返回 JWT Token。
* `POST /api/auth/login/`：**传统账密登录**。获取 JWT Token。
* `POST /api/auth/face-register/`：**人脸特征绑定接口 (录入)**。接收边缘端传来的截帧 Base64，由本地 ONNX 引擎提取 512 维特征向量，存入 `UserFaceEmbedding`。
* `POST /api/auth/face-login/`：**无感人脸登录 (1:N 检索)**。接收边缘端实时的 Bbox 和 Base64 截帧，后端与数据库遍历计算余弦相似度，防误判拦截（如未正视、距离过远），匹配成功即发放 JWT Token 并触发硬件底层 TTS 欢迎。

### 模块 2：数据查询与聚合 (Data & Dashboard)

* `GET /api/user/dashboard/`：**主页聚合接口**。一次性拉取基础档案、今日计划，并内置“今日任务完成度算法”，动态计算实际运动记录与目标计划的重合度（百分比）。
* `GET /api/user/load/`：**滚动负荷计算**。自动查询并聚合过去 7 天的运动耗时与强度，返回综合健康状态评估。
* `GET /api/user/activities/`：**宏观历史记录**。获取轻量级的历史训练列表。
* `GET /api/user/activities/<id>/`：**单次训练深度详情 (含心率血氧图谱)**。不仅返回表单数据，还会全量返回 `time_series` 传感器数组，供前端 ECharts 渲染恢复曲线，并附带大模型生成的 `ai_report` 评语。

### 模块 3：训练计划管理 (Plan Engine)

* `POST /api/plan/init-generate/`：**AI 计划初始化**。传入自然语言目标，调用本地大模型生成结构化 JSON 排期。
* `GET / POST /api/plan/current/`：**当前激活计划管理**。支持读取当前正在执行的计划，或接受前端传来的新 JSON 数组手动覆写并激活新计划。
* `GET /api/train/exercises/`：**【新增】动作字典库**。暴露系统目前支持的所有运动列表，供前端在“自由训练”模式下生成下拉菜单。

### 模块 4：核心训练中枢与 LLM (Train & AI)

* `POST /api/chat/ask/`：**RAG 私教问答**。检索用户最近 7 天的真实宏观数据与上一次的教练评语作为背景知识，拒绝大模型“幻觉”瞎编。
* `POST /api/train/micro-coach/`：**组间话疗接口**。根据抓取到的动作变形错误码，调用轻量 LLM 快速生成短文本，直通底层 TTS 播报。
* `POST /api/train/finish/`：**🏅 核心异步结算接口**。
* **Payload (防网络阻塞设计)**：接收运动耗时、动作错误次数、客观强度，以及**在前端内存中暂存的 `time_series`（心率血氧数组）**。
* **响应分离**：Django 立即落盘基础数据与高频数据，返回 `HTTP 202 Accepted` 以及 `activity_id`，绝不阻塞 Kiosk 前端画面。并在此刻**唤起后台独立大模型评估线程**。


* `GET /api/train/status/<id>/`：**【新增】AI 分析状态轮询**。配合上述异步结算接口使用。前端拿到 `activity_id` 后开启 3 秒一轮的静默轮询，直到后端返回 `status: COMPLETED`，前端立刻弹出“报告已生成”的气泡通知并播放语音。

---

## 伍、 后端业务框架 (Django + 本地 LLM 协同)

### 1. 大模型配置解耦 (YAML 静态配置)

在 `config/llm_rules.yaml` 中维护动作库字典、判定阈值以及 Prompt 模板骨架。`services/llm_service.py` 运行时直接读取 YAML 进行动态变量注入，支持随时热调参，无需重启 Django 服务。

### 2. 异步大模型协同工作流 (LLM Service Layer)

大模型的推理可能耗时数秒到十几秒，为保障边缘设备的极度流畅，核心链路（特别是运动后结算）采用 **异步非阻塞 (Async/Thread) + 状态轮询 (Polling)** 的企业级架构。

在 `services/llm_service.py` 和 `api/views.py` 中，划分为以下场景：

* **场景一：组间休息极速短评 (Micro-Coaching)**
向本地 LLM 请求极短的 `max_tokens`，确保存几秒内返回文字供底层转换语音，不阻塞下一组运动倒计时。
* **场景二：复盘评估与计划自进化 (Post-Workout Auto-Update)**
* **影子线程运作**：在 `TrainFinishView` 中触发，由 `threading.Thread` 在后台静默运行。
* **主客观交叉验证**：后台线程通过 ORM 动态聚合刚刚入库的 `ActivityTimeSeries`，得出真实的“恢复期平均心率”与“最低血氧”，加上用户表单提交的主观疲劳度 (RPE)，组装成超级 Prompt 交给大模型。
* **数据库静默覆写**：大模型生成结论后，线程主动写入 `AIFeedback` 表并更新主表的 `quality_score`；如果触发了负荷调整，线程将直接覆写作废旧的 `TrainingPlan` 并插入新计划。整个过程零人工干预，直到前端轮询探测到完成。


* **场景三：真实感 RAG 数据伴侣 (Local Data Chatbot)**
* 利用 `Sum` 等 Django 聚合函数，动态提取近 7 天的总耗时、占比最高的训练强度（如“高强度”），以及上一次的私教评语，注入到聊天 Prompt 模板中。这赋予了体测机本地私有且高度精准的记忆能力。



### 3. 数据隔离与健壮性保障

* **心率血氧降压**：绝不开启实时独立接口上传心率血氧。硬件数据缓存在前端 JS 内存中，直到训练 `END` 后打包批量上传（Bulk Create）。
* **连接池防漏**：在后台所有大模型计算线程结束（或发生异常崩溃）时，通过 `try...finally` 与 `connection.close()` 强制回收 Django 数据库连接，杜绝设备长时间运行后出现的 `too many clients` 错误。
* **JSON 容错正则提取**：在所有大模型生成结构化 JSON 的场景中，引入 `re.DOTALL` 正则贪婪匹配，智能剔除大模型可能吐出的前置解释废话（如 "好的，为您生成如下计划："），极大降低 JSON 反序列化崩溃率。